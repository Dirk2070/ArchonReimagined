{
  "buildCommand": "pnpm -w --filter web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm -w install --frozen-lockfile",
  "devCommand": "pnpm -w --filter web dev",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
